---
title: Home
blocks:
  - height: 45vw
    max_height: 900px
    min_height: 200px
    item:
      - src: /uploads/Behind the scenes shots - @theuncannydom-33.jpg
        title: Portfolio
        text: |
          I'll let my work speak for itself
        button:
          text: See my Work
          url: /websites
        custom_css: |-
          == .image--image {
            object-position: 50% 30%;
          }
    _template: hero
  - title: Skills
    text: |
      Web Development
    item:
      - title: HTML
      - title: CSS / SCSS
      - title: JavaScript
      - title: jQuery
      - title: Shopify
      - title: Liquid
      - title: Gulp
      - title: JSON
      - title: TypeScript
      - title: Jekyll
      - title: Node.js
      - title: npm
      - title: YAML
      - title: Yarn
      - title: React
      - title: Next.js
      - title: GraphQL
    custom_css: |
      == {
        --page-width: var(--page-width--text);
        border-bottom: 0;
        --cta-set--padding-bottom: 0;
      }

      == .section--title {
        text-align: left;
        max-width: 100%;
        --title-set--outer-padding: calc(var(--page--padding) * 0.125);
      }

      @media screen and (min-width: 750px) {
        == .section--title {
          --title-set--outer-padding: calc(var(--page--padding) * 0.25);
        }
      }

      == .section--title .rte {
        font-weight: 600;
        font-family: var(--font-heading--family);;
        font-size: var(--theme--font-size--h2);
      }

      == .cell {
        --grid: var(--sp--2-3);
        --grid-bottom: var(--sp--2-3);

        --grid--mobile: var(--sp);
        --grid-bottom--mobile: var(--sp);
      }
      
      == .cell__item {
        --width: fit-content;
        --font-heading--family: var(--font-body--family);
        --font-heading--weight: 400;
      }

      == .ctas--text {
        --theme--font-size--h2: var(--theme--font-size--h3);
      }
    _template: ctas
  - text: |
      Programming Languages
    item:
      - title: C++
      - title: Ruby
      - title: PHP
    custom_css: |
      @media screen and (min-width: 750px) {
        == {
          --page-width: var(--page-width--text);
          --cta-set--padding-top: calc(var(--page--padding) * 0.66);
        }
      }

      == .section--title {
        text-align: left;
        max-width: 100%;
        --title-set--outer-padding: calc(var(--page--padding) * 0.25);
      }

      == .section--title .rte {
        font-weight: 600;
        font-family: var(--font-heading--family);;
        font-size: var(--theme--font-size--h2);
      }

      == .cell {
        --grid: var(--sp--2-3);
        --grid-bottom: var(--sp--2-3);

        --grid--mobile: var(--sp);
        --grid-bottom--mobile: var(--sp);
      }
      
      == .cell__item {
        --width: fit-content;
        --font-heading--family: var(--font-body--family);
        --font-heading--weight: 400;
      }

      == .ctas--text {
        --theme--font-size--h2: var(--theme--font-size--h3);
      }
    _template: ctas

  - title: Tools
    text: |
      Development
    item:
      - title: iTerm
      - title: Sublime Text 3
      - title: VSCode
      - title: GitHub
      - title: GitLab
      - title: Heroku
      - title: Forestry
      - title: TinaCMS
    custom_css: |
      == {
        --page-width: var(--page-width--text);
        border-bottom: 0;
        --cta-set--padding-bottom: 0;
      }

      == .section--title {
        text-align: left;
        max-width: 100%;
        --title-set--outer-padding: calc(var(--page--padding) * 0.25);
      }

      == .section--title .rte {
        font-weight: 600;
        font-family: var(--font-heading--family);;
        font-size: var(--theme--font-size--h2);
      }

      == .cell {
        --grid: var(--sp--2-3);
        --grid-bottom: var(--sp--2-3);

        --grid--mobile: var(--sp);
        --grid-bottom--mobile: var(--sp);
      }
      
      == .cell__item {
        --width: fit-content;
        --font-heading--family: var(--font-body--family);
        --font-heading--weight: 400;
      }

      == .ctas--text {
        --theme--font-size--h2: var(--theme--font-size--h3);
      }
    _template: ctas
  - text: |
      Design
    item:
      - title: Photoshop
      - title: Illustrator
      - title: Premiere Pro
      - title: InDesign
      - title: Sketch
      - title: Figma
    custom_css: |
      == {
        --page-width: var(--page-width--text);
        border-bottom: 0;
        --cta-set--padding-bottom: 0;
      }

      @media screen and (min-width: 750px) {
        == {
          --page-width: var(--page-width--text);
          --cta-set--padding-top: calc(var(--page--padding) * 0.66);
        }
      }

      == .section--title {
        text-align: left;
        max-width: 100%;
        --title-set--outer-padding: calc(var(--page--padding) * 0.25);
      }

      == .section--title .rte {
        font-weight: 600;
        font-family: var(--font-heading--family);;
        font-size: var(--theme--font-size--h2);
      }

      == .cell {
        --grid: var(--sp--2-3);
        --grid-bottom: var(--sp--2-3);

        --grid--mobile: var(--sp);
        --grid-bottom--mobile: var(--sp);
      }
      
      == .cell__item {
        --width: fit-content;
        --font-heading--family: var(--font-body--family);
        --font-heading--weight: 400;
      }

      == .ctas--text {
        --theme--font-size--h2: var(--theme--font-size--h3);
      }
    _template: ctas
  - text: |
      3D and Game Development
    item:
      - title: Unreal Engine
      - title: Blender
    custom_css: |
      @media screen and (min-width: 750px) {
        == {
          --page-width: var(--page-width--text);
          --cta-set--padding-top: calc(var(--page--padding) * 0.66);
        }
      }

      == .section--title {
        text-align: left;
        max-width: 100%;
        --title-set--outer-padding: calc(var(--page--padding) * 0.25);
      }

      == .section--title .rte {
        font-weight: 600;
        font-family: var(--font-heading--family);;
        font-size: var(--theme--font-size--h2);
      }

      == .cell {
        --grid: var(--sp--2-3);
        --grid-bottom: var(--sp--2-3);

        --grid--mobile: var(--sp);
        --grid-bottom--mobile: var(--sp);
      }
      
      == .cell__item {
        --width: fit-content;
        --font-heading--family: var(--font-body--family);
        --font-heading--weight: 400;
      }

      == .ctas--text {
        --theme--font-size--h2: var(--theme--font-size--h3);
      }
    _template: ctas
---

# **Homepage** is *Homepage*.

She must have hidden the plans in the escape pod. Send a detachment down to retrieve them, and see to it personally, Commander. There'll be no one to stop us this time!
